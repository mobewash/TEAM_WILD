<%= form_for(@color) do |f| %>
  <% if @color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@color.errors.count, "error") %> prohibited this color from being saved:</h2>

      <ul>
      <% @color.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :favColor %><br>
    <%= f.text_field :favColor %>
  </div>
  <div class="field">
    <%= f.label :favNum %><br>
    <%= f.number_field :favNum %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
